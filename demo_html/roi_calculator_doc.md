# 经济效益计算器技术文档

## 1. 概述

本文档详细说明 `roi_calculator.html` 的计算逻辑、参数定义、口径说明和实现细节。

**计算器功能**：
- 年度节省（Annual Savings）
- 回本周期（Payback Period）
- 内部收益率（IRR - Internal Rate of Return）
- 净现值（NPV - Net Present Value）

**支持模式**：
- 净计量（Net Metering）/ 非净计量（Non-Net Metering）
- 电池更换方式：按月计提备用金 / 一次性支付

---

## 2. 输入参数定义

### 2.1 基础参数

| 参数 | 符号 | 单位 | 说明 | 默认值 |
|------|------|------|------|--------|
| 年度用电量 | E | kWh/年 | 用户年度总用电量 | 5000 |
| 购电价 | x | $/kWh | 从电网购电的单价（含税） | 0.30 |
| 固定日费 | z | $/天 | 电网固定日费（connection fee） | 1.00 |
| 上网电价 | y | $/kWh | 售电给电网的单价（feed-in tariff） | 0.08 |
| 电价通胀率 | q | % | 年度电价增长率 | 3% |

### 2.2 系统参数

| 参数 | 符号 | 单位 | 说明 | 默认值 |
|------|------|------|------|--------|
| 系统一次性成本 | H | $ | 光伏系统初始投资（含安装） | 10000 |
| 额定年发电量 | P | kWh/年 | 系统额定发电量（未计衰减） | 6000 |
| 自用率 | a | % | 发电量中用户自用的比例 | 60% |
| 首年衰减率 | r' | % | 第一年功率衰减率 | 2% |
| 后续衰减率 | r'' | % | 第二年起每年衰减率 | 0.5% |

### 2.3 电池参数

| 参数 | 符号 | 单位 | 说明 | 默认值 |
|------|------|------|------|--------|
| 电池更换成本 | H_battery | $ | 电池更换一次的成本 | 3000 |
| 电池更换年限 | N_battery | 年 | 第几年需要更换电池 | 10 |

---

## 3. 核心计算逻辑

### 3.1 发电量衰减模型

**第 n 年实际发电量 P_n**：

```
P_1 = P × (1 - r')                    // 第1年
P_n = P × (1 - r') × (1 - r'')^(n-1)  // 第n年 (n ≥ 2)
```

**示例**（P=6000, r'=2%, r''=0.5%）：
- 第1年：6000 × 0.98 = 5880 kWh
- 第2年：6000 × 0.98 × 0.995 = 5850.6 kWh
- 第10年：6000 × 0.98 × 0.995^9 = 5621.8 kWh

### 3.2 电量分配

**第 n 年电量分配**：

```
U_n = min(a × P_n, E)           // 自用电量
S_n = max(P_n - U_n, 0)         // 售电量（上网）
D_n = max(E - U_n, 0)           // 购电量（从电网）
```

**关系验证**：
- `U_n + S_n = P_n`（发电量 = 自用 + 售出）
- `U_n + D_n = E`（用电量 = 自用 + 购入）

### 3.3 电费计算

#### 3.3.1 未装光伏的年度电费 k_n

```
A = E × x + 365 × z                    // 基准年度电费
k_n = A × (1 + q)^n                    // 第n年电费（考虑通胀）
```

#### 3.3.2 装光伏后的年度电费 J_n

**模式1：非净计量（Non-Net Metering）**

```
C_n = (D_n × x + 365 × z) × (1 + q)^n  // 购电成本 + 固定费
G_n = S_n × y                           // 售电收入（不随通胀）
J_n = C_n - G_n                         // 净电费
```

**特点**：
- 购电和售电独立计算
- 售电收入固定单价（y 不随通胀）
- 固定日费仍需支付

**模式2：净计量（Net Metering）**

```
能量费用 = max(D_n - S_n, 0) × x × (1 + q)^n
固定日费 = 365 × z × (1 + q)^n
馈网收入 = max(S_n - D_n, 0) × y
J_n = 能量费用 + 固定日费 - 馈网收入
```

**特点**：
- 购电和售电可以抵消
- 只有净购电量（D_n - S_n > 0）才计费
- 净售电量（S_n - D_n > 0）才有收入
- 固定日费仍需支付

### 3.4 年度节省 AS_n

```
AS_n = k_n - J_n
```

**说明**：
- 未考虑货币时间价值（不贴现）
- 直接对比同年度的电费差额

---

## 4. 回本周期计算

### 4.1 计算原理

**目标**：找到累计现金流首次 ≥ 0 的时间点

**初始状态**：
```
累计现金流 = -H（系统成本）
```

**月度迭代**（最多240个月）：
```
第 m 月所在年份 n = ceil(m / 12)
月度现金流 = AS_n / 12 - 电池月度成本
累计现金流 += 月度现金流

if 累计现金流 ≥ 0:
    回本月数 = m
    回本年数 = m / 12
```

### 4.2 电池成本处理

#### 口径1：按月计提备用金

```
月度备用金 = H_battery / (N_battery × 12)
```

**示例**（H_battery=3000, N_battery=10）：
- 每月扣除：3000 / 120 = $25
- 前120个月持续扣除
- 第121个月起不再扣除

**特点**：
- 平滑现金流
- 提前准备电池更换资金
- 更保守的回本预测

#### 口径2：一次性支付

```
if m == N_battery × 12:
    月度现金流 -= H_battery
```

**示例**（H_battery=3000, N_battery=10）：
- 第120个月一次性扣除 $3000
- 其他月份不扣除

**特点**：
- 符合实际支付时点
- 回本曲线有明显跳变
- 更乐观的前期回本预测

### 4.3 输出格式

```
回本周期 < 1个月：  "立即回本"
回本周期 ≤ 240个月： "X.XX 年（≈ Y 个月）"
回本周期 > 240个月： "大于20年"
```

---

## 5. IRR 计算

### 5.1 现金流构建

**0-20年现金流序列**：

```
CF_0 = -H                              // 初始投资
CF_n = AS_n - 电池投入_n (n=1..20)    // 年度现金流

其中：
电池投入_n = H_battery  (n == N_battery)
电池投入_n = 0          (n != N_battery)
```

**示例**（H=10000, H_battery=3000, N_battery=10）：
```
CF_0  = -10000
CF_1  = AS_1
CF_2  = AS_2
...
CF_10 = AS_10 - 3000   // 电池更换年
CF_11 = AS_11
...
CF_20 = AS_20
```

### 5.2 IRR 求解

**定义**：使净现值（NPV）为零的折现率 r

```
NPV(r) = Σ(n=0 to 20) CF_n / (1 + r)^n = 0
```

**求解方法**：二分法（Bisection Method）

```javascript
function irr(cfs) {
  let low = -0.99, high = 10.0
  
  // 计算NPV函数
  const npv = (r) => cfs.reduce((sum, cf, i) => 
    sum + cf / Math.pow(1 + r, i), 0)
  
  // 扩展搜索区间（如果需要）
  let fL = npv(low), fH = npv(high)
  while (fL * fH > 0 && expand < 20) {
    high *= 2
    fH = npv(high)
    expand++
  }
  
  // 二分法迭代
  for (let iter = 0; iter < 100; iter++) {
    const mid = (low + high) / 2
    const fM = npv(mid)
    
    if (Math.abs(fM) < 1e-6) return mid
    
    if (fL * fM < 0) {
      high = mid
      fH = fM
    } else {
      low = mid
      fL = fM
    }
  }
  
  return null  // 无解
}
```

**收敛条件**：
- |NPV(r)| < 1e-6
- 最大迭代次数：100次

### 5.3 IRR 解读

| IRR 范围 | 投资评价 | 说明 |
|----------|----------|------|
| IRR < 0% | 亏损 | 20年内无法收回投资 |
| 0% ≤ IRR < 5% | 较低收益 | 勉强回本，收益不理想 |
| 5% ≤ IRR < 10% | 中等收益 | 合理的投资回报 |
| IRR ≥ 10% | 高收益 | 优秀的投资项目 |

---

## 6. NPV 计算

### 6.1 NPV 定义

**净现值（Net Present Value, NPV）**：将未来现金流按贴现率折现到当前时点的总价值。

**公式**：
```
NPV = -H + Σ(n=1 to T) CF_n / (1 + d)^n

其中：
H = 初始投资
CF_n = 第n年净现金流
d = 贴现率（discount rate）
T = 项目年限（默认25年）
```

**与IRR的区别**：
- IRR：求解使NPV=0的贴现率（内部收益率）
- NPV：给定贴现率，计算项目的绝对价值

### 6.2 现金流构建

**年度净现金流 CF_n**：
```
CF_n = AS_n - 电池投入_n - 运维成本_n

其中：
AS_n = 年度节省（见3.4节）
电池投入_n = H_battery（当 n == N_battery 时）
运维成本_n = 年度运维费用（可选）
```

**考虑因素**：
1. **电价增长**：AS_n 随电价通胀逐年增加
2. **系统衰减**：发电量逐年下降，影响 AS_n
3. **电池更换**：第 N_battery 年扣除 H_battery
4. **贴现率**：反映资金时间价值和投资风险

### 6.3 计算示例

**输入参数**：
```
H = $10000（初始投资）
AS_1 = $1615（首年节省）
H_battery = $3000
N_battery = 10年
d = 5%（贴现率）
T = 25年
```

**现金流序列**：
```
CF_0  = -$10000（初始投资）
CF_1  = $1615 / (1.05)^1 = $1538
CF_2  = $1635 / (1.05)^2 = $1483
...
CF_10 = ($1755 - $3000) / (1.05)^10 = -$764
CF_11 = $1775 / (1.05)^11 = $1032
...
CF_25 = $2115 / (1.05)^25 = $626
```

**NPV 计算**：
```
NPV = -10000 + Σ(折现后的现金流)
    = -10000 + 1538 + 1483 + ... + 626
    ≈ $15,234
```

### 6.4 NPV 解读

| NPV 值 | 投资决策 | 说明 |
|--------|----------|------|
| NPV > 0 | 可行 | 项目创造正价值，建议投资 |
| NPV = 0 | 临界点 | 项目刚好达到要求回报率 |
| NPV < 0 | 不可行 | 项目无法达到要求回报率 |

**NPV 与 IRR 对比**：

| 指标 | NPV | IRR |
|------|-----|-----|
| **含义** | 绝对价值（美元） | 相对收益率（百分比） |
| **决策标准** | NPV > 0 | IRR > 要求回报率 |
| **优势** | 直观反映价值创造 | 便于比较不同规模项目 |
| **劣势** | 需要预设贴现率 | 可能存在多解或无解 |
| **适用场景** | 单一项目评估 | 多项目排序比较 |

### 6.5 贴现率选择

**常见贴现率参考**：

| 贴现率 | 适用场景 | 风险水平 |
|--------|----------|----------|
| 3-5% | 保守型投资者，低风险偏好 | 低 |
| 5-8% | 中等风险投资者，市场平均回报 | 中 |
| 8-12% | 激进型投资者，高收益要求 | 高 |

**贴现率考虑因素**：
1. **无风险利率**：国债收益率（基准）
2. **通货膨胀**：货币购买力下降
3. **机会成本**：资金的其他投资机会
4. **项目风险**：光伏系统特有风险溢价

**示例**：
```
贴现率 = 无风险利率 + 风险溢价
       = 3%（国债） + 2%（光伏风险）
       = 5%
```

### 6.6 敏感性分析

**NPV 对贴现率的敏感性**：

| 贴现率 | NPV | 评价 |
|--------|-----|------|
| 3% | $18,500 | 高价值 |
| 5% | $15,234 | 中等价值 |
| 8% | $10,800 | 较低价值 |
| 10% | $8,200 | 临界可行 |
| 12% | $5,500 | 勉强可行 |

**结论**：
- 贴现率越高，NPV 越低
- 当贴现率 = IRR 时，NPV = 0
- 项目应在多个贴现率下评估稳健性

### 6.7 实现代码

```javascript
function calculateNPV(annualSavings, initialInvestment, batteryReplacement, 
                      batteryYear, discountRate, projectYears) {
  let npv = -initialInvestment;
  
  for (let year = 1; year <= projectYears; year++) {
    // 年度净现金流
    let cashFlow = annualSavings[year - 1];
    
    // 扣除电池更换成本
    if (year === batteryYear) {
      cashFlow -= batteryReplacement;
    }
    
    // 折现到当前价值
    const discountedCashFlow = cashFlow / Math.pow(1 + discountRate, year);
    npv += discountedCashFlow;
  }
  
  return npv;
}
```

**使用示例**：
```javascript
const annualSavings = [1615, 1635, 1655, ...]; // 25年数据
const npv = calculateNPV(
  annualSavings,
  10000,    // 初始投资
  3000,     // 电池成本
  10,       // 电池更换年份
  0.05,     // 5%贴现率
  25        // 25年项目期
);

console.log(`NPV: $${npv.toFixed(2)}`);
```

---

## 7. 计算口径对比

### 7.1 净计量 vs 非净计量

| 维度 | 净计量（Net Metering） | 非净计量（Non-Net Metering） |
|------|------------------------|------------------------------|
| **购售电抵消** | 可抵消（净购电量计费） | 独立计算（不可抵消） |
| **适用场景** | 政策支持地区 | 无净计量政策地区 |
| **经济性** | 更优（可抵消购电） | 较差（售电价低于购电价） |
| **计算复杂度** | 中等 | 简单 |
| **公式** | `J_n = max(D_n-S_n,0)×x×infl + 365z×infl - max(S_n-D_n,0)×y` | `J_n = (D_n×x + 365z)×infl - S_n×y` |

**示例对比**（E=5000, P_1=5880, a=60%, x=0.3, y=0.08, z=1, q=3%）：

```
第1年：
U_1 = 3528 kWh
S_1 = 2352 kWh
D_1 = 1472 kWh

非净计量：
  C_1 = (1472×0.3 + 365×1) × 1.03 = $831.05
  G_1 = 2352 × 0.08 = $188.16
  J_1 = $831.05 - $188.16 = $642.89

净计量：
  能量费用 = 0（因为 S_1 > D_1）
  固定日费 = 365 × 1 × 1.03 = $375.95
  馈网收入 = (2352-1472) × 0.08 = $70.40
  J_1 = $0 + $375.95 - $70.40 = $305.55

年度节省差异：
  非净计量：k_1 - $642.89 = $897.11
  净计量：k_1 - $305.55 = $1234.45
  差额：$337.34（净计量更优）
```

### 7.2 电池口径对比

| 维度 | 按月计提 | 一次性支付 |
|------|----------|------------|
| **现金流特征** | 平滑 | 跳变 |
| **回本周期** | 更长（保守） | 更短（乐观） |
| **财务规划** | 提前准备 | 集中支付 |
| **适用场景** | 稳健型投资者 | 激进型投资者 |
| **月度扣除** | `H_battery / (N_battery × 12)` | 第 `N_battery × 12` 月扣除 `H_battery` |

**示例对比**（H=10000, AS_1=1200, H_battery=3000, N_battery=10）：

```
按月计提：
  月度现金流 = 1200/12 - 3000/120 = $100 - $25 = $75
  前120个月累计 = -10000 + 75×120 = -$1000
  回本月数 ≈ 133个月（11.1年）

一次性支付：
  前119个月：月度现金流 = $100
  第120个月：月度现金流 = $100 - $3000 = -$2900
  前119个月累计 = -10000 + 100×119 = $900
  第120个月后 = $900 - $2900 = -$2000
  回本月数 ≈ 140个月（11.7年）
```

---

## 8. 计算假设与限制

### 8.1 核心假设

1. **时间粒度**：年度计算，月度回本周期采用年度均摊
2. **通胀范围**：仅电价通胀，系统成本和售电价不通胀
3. **衰减模型**：线性衰减，不考虑设备故障
4. **自用率恒定**：a 在20年内保持不变
5. **无其他收益**：不考虑政府补贴、税收优惠等
6. **无维护成本**：除电池更换外，不计其他运维成本
7. **电池更换一次**：仅考虑第 N_battery 年更换一次

### 8.2 边界情况

| 场景 | 处理方式 | 说明 |
|------|----------|------|
| P_n < E | 部分自用，部分购电 | 正常计算 |
| P_n ≥ E | 全部自用，剩余上网 | S_n = P_n - E, D_n = 0 |
| a = 100% | 全部自用，无上网 | S_n = 0 |
| a = 0% | 全部上网，无自用 | U_n = 0, S_n = P_n |
| H_battery = 0 | 无电池更换 | 回本周期不扣电池成本 |
| N_battery > 20 | 20年内不更换 | IRR 不扣电池成本 |

### 8.3 已知限制

1. **不支持动态电价**：无法处理峰谷电价、时段电价
2. **不支持多次电池更换**：仅支持一次更换
3. **不支持贴现率调整**：IRR 计算固定使用内部收益率
4. **不支持月度粒度**：年度节省无法精确到月
5. **不支持系统扩容**：假设系统容量20年不变

---

## 9. 使用示例

### 9.1 典型住宅场景

**输入参数**：
```
E = 8000 kWh/年
x = $0.35/kWh
z = $1.20/天
y = $0.10/kWh
q = 4%
H = $15000
P = 10000 kWh/年
a = 70%
r' = 2%
r'' = 0.5%
H_battery = $4000
N_battery = 10年
模式 = 净计量
电池口径 = 按月计提
```

**预期输出**：
```
年度节省：$1800 - $2200（首年）
回本周期：8-10年
IRR：8% - 12%
```

### 9.2 高电价场景

**输入参数**：
```
E = 6000 kWh/年
x = $0.45/kWh（高电价）
z = $1.50/天
y = $0.08/kWh
q = 5%
H = $12000
P = 7000 kWh/年
a = 80%（高自用率）
```

**预期输出**：
```
年度节省：$2000+（首年）
回本周期：5-7年
IRR：12% - 18%（优秀投资）
```

### 9.3 非净计量场景

**输入参数**：
```
E = 5000 kWh/年
x = $0.30/kWh
y = $0.05/kWh（低售电价）
模式 = 非净计量
```

**预期输出**：
```
年度节省：比净计量低 20%-30%
回本周期：比净计量长 2-3年
IRR：比净计量低 2%-4%
```

---

## 10. 技术实现

### 10.1 核心函数

```javascript
// 发电量衰减
function pn_year(P, r1, r2, year)

// 年度计算
function computeYear(E, x, z, y, q, P, a, r1, r2, year, mode)

// 回本周期
function paybackMonths(E, x, z, y, q, P, a, r1, r2, H, Hbat, Nbat, mode, pbMode)

// IRR求解
function irr(cfs)
```

### 10.2 数据流

```
用户输入参数
    ↓
点击"计算"按钮
    ↓
读取所有输入值（val函数）
    ↓
计算首年结果（computeYear）→ 年度节省
    ↓
计算回本周期（paybackMonths）→ 回本周期
    ↓
构建现金流序列（1-20年）→ IRR
    ↓
渲染计算过程（3个表格）
    ↓
更新KPI卡片显示
```

### 10.3 性能特征

- **计算复杂度**：O(n)，n=20年
- **IRR求解**：O(100)，最多100次迭代
- **回本周期**：O(240)，最多240个月
- **总计算时间**：< 10ms（现代浏览器）

---

## 11. 测试用例

### 11.1 基准测试

**输入**：
```javascript
{
  E: 5000, x: 0.30, z: 1, y: 0.08, q: 0.03,
  H: 10000, P: 6000, a: 0.60,
  r1: 0.02, r2: 0.005,
  Hbat: 3000, Nbat: 10,
  mode: 'net', pbMode: 'amortized'
}
```

**预期输出**：
```javascript
{
  年度节省: "$1019",
  回本周期: "11.08 年 (≈ 133 个月)",
  IRR: "6.45%"
}
```

### 11.2 边界测试

| 测试场景 | 输入 | 预期输出 |
|----------|------|----------|
| 零成本系统 | H=0 | 回本周期="立即回本", IRR=无穷大 |
| 零发电量 | P=0 | 年度节省=$0, 回本周期="大于20年" |
| 100%自用 | a=1.0 | S_n=0（无上网） |
| 0%自用 | a=0 | U_n=0（全部上网） |
| 无电池 | Hbat=0 | 回本周期更短 |

---

## 12. 维护指南

### 12.1 参数调整

**修改默认值**：
```html
<input id="E" type="number" value="5000" />
```

**修改计算年限**（默认20年）：
```javascript
for(let n=1; n<=20; n++) {  // 改为其他年限
```

### 12.2 公式更新

**修改衰减模型**：
```javascript
function pn_year(P, r1, r2, year) {
  // 在此修改衰减公式
}
```

**修改通胀模型**：
```javascript
const infl = Math.pow(1+q, year)  // 复利通胀
// 改为：const infl = 1 + q*year  // 简单通胀
```

### 12.3 新增模式

**添加新的计量模式**：
```html
<select id="mode">
  <option value="net">净计量</option>
  <option value="non_net">非净计量</option>
  <option value="new_mode">新模式</option>
</select>
```

```javascript
if(mode === 'new_mode') {
  // 新模式计算逻辑
}
```

---

## 13. 参考资料

### 13.1 公式来源

- 核心公式：`方案计算 1014/经济效益计算.md`
- 系统规则：`方案计算 1014/计算规则.md`

### 13.2 相关标准

- IRR计算：XIRR (Excel函数标准)
- 净计量政策：各州电力公司规定
- 衰减率标准：光伏组件厂商质保条款

### 13.3 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0 | 2025-01-14 | 初始版本，基础计算功能 |
| v2.0 | 2025-01-15 | 优化表格显示，增加中文说明，拆分IRR明细 |
| v3.0 | 2025-01-16 | 新增NPV计算功能，包含贴现率选择和敏感性分析 |

---

## 附录：完整计算示例

### 输入参数
```
E = 5000 kWh/年
x = $0.30/kWh
z = $1.00/天
y = $0.08/kWh
q = 3%
H = $10000
P = 6000 kWh/年
a = 60%
r' = 2%
r'' = 0.5%
H_battery = $3000
N_battery = 10年
模式 = 净计量
电池口径 = 按月计提
```

### 第1年详细计算

**1. 发电量**
```
P_1 = 6000 × (1 - 0.02) = 5880 kWh
```

**2. 电量分配**
```
U_1 = min(0.6 × 5880, 5000) = 3528 kWh
S_1 = 5880 - 3528 = 2352 kWh
D_1 = 5000 - 3528 = 1472 kWh
```

**3. 未装电费**
```
A = 5000 × 0.30 + 365 × 1 = $1865
k_1 = 1865 × 1.03 = $1920.95
```

**4. 装后电费（净计量）**
```
能量费用 = max(1472 - 2352, 0) × 0.30 × 1.03 = $0
固定日费 = 365 × 1 × 1.03 = $375.95
馈网收入 = max(2352 - 1472, 0) × 0.08 = $70.40
J_1 = 0 + 375.95 - 70.40 = $305.55
```

**5. 年度节省**
```
AS_1 = 1920.95 - 305.55 = $1615.40
```

### 20年现金流

| 年 | P_n | AS_n | 电池投入 | CF_n |
|----|-----|------|----------|------|
| 0 | - | - | $10000 | -$10000 |
| 1 | 5880 | $1615 | $0 | $1615 |
| 2 | 5851 | $1635 | $0 | $1635 |
| ... | ... | ... | ... | ... |
| 10 | 5622 | $1755 | $3000 | -$1245 |
| 11 | 5594 | $1775 | $0 | $1775 |
| ... | ... | ... | ... | ... |
| 20 | 5374 | $1915 | $0 | $1915 |

### 最终结果

```
年度节省（首年）：$1615
回本周期：11.08 年（≈ 133 个月）
IRR：6.45%
```
